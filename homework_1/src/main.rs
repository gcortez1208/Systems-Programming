fn assignment1(){
    //temp converter
    let const feezingPoint;
}

 fn fahrenheit_to_celsius(f:f64)->f64{
    let celsius=(f-32)*(5/9)
    return celsius;
 }

 fn celsius_to_fahrenheit(c:f64)->f64{
    let fahrenheit=(f*(9/5)+32);
    return fahrenheit;
 }

fn main(){
    let mut x=32;
    fahrenheit_to_celsius(x);
    while i<5{
        println!
    }
    assignment1();
}